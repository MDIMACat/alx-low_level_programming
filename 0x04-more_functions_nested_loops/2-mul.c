#include "main.h"
/**
 * mul - multiples two integers together
 * @a: is a parameter for function
 * @b: is a parameter for function
 *
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int a;
	int b;
	int sum;

	sum = a * b;
	_putchar(sum + '0');

	return (0);
}
